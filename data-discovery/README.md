## To run:

1. Create a virtual environment in this folder:

```bash
python -m venv .venv
```

2. Activate the virtual environment:

```bash
source .venv/Scripts/activate (or source .venv/bin/activate or another equivalent command)
```

3. Install the required dependencies

```bash
pip install .
```
