//! `auditrs` rewrite of the `ausearch` utility.
//!
//! This tool will eventually provide rich search capabilities over audit logs
//! (and possibly live streams), exposing filters that align with the internal
//! `rules` model instead of the raw auditd flags.
